import SignupForm from "@/components/Signup";

// return signup up form for login page
export default function LoginPage() {
    return (
        <main>
            <SignupForm/>
        </main>
    )
}